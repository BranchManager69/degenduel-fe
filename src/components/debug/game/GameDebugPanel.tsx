import React from "react";
////import { WebSocketMonitor } from "./WebSocketMonitor";

/**
 * This will be the main debug panel for the main game's frontend, backend, and everything in between
 */
export const GameDebugPanel: React.FC = () => {
  return (
    <div className="game-debug-panel">
      <GameDebugPanel />
    </div>
  );
};
